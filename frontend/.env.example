# The backend URL the Vite dev-server proxy will forward /api and /storage to.
# This is a SERVER-SIDE variable (no VITE_ prefix) — it is never exposed to the browser.
# Copy this file to .env and set it to your backend URL.
API_BACKEND_URL=https://blueviolet-newt-360669.hostingersite.com
